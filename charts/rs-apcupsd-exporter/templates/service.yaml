apiVersion: v1
kind: Service
metadata:
  name: {{ include "rs-apcupsd-exporter.fullname" . }}
  labels:
    {{- include "rs-apcupsd-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rs-apcupsd-exporter.selectorLabels" . | nindent 4 }}
